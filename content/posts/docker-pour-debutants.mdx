---
title: "Docker expliqué simplement : Ton premier conteneur en 10 minutes"
description: "Comprends Docker facilement et lance ton premier conteneur. Guide complet pour débutants avec exemples pratiques et commandes essentielles."
date: "2025-01-25"
category: "devops"
tags: ["docker", "conteneurs", "devops", "débutant"]
author: "BiLearn"
youtubeId: "dQw4w9WgXcQ"
---

## Introduction

**Docker** est devenu incontournable dans le monde du développement et du DevOps. 
Mais c'est quoi exactement ? Et pourquoi tout le monde en parle ? 🐳

Dans ce guide, je vais t'expliquer Docker de manière simple, avec des analogies concrètes 
et des exemples pratiques.

## 🤔 C'est quoi Docker en fait ?

Imagine que tu veux déménager. Tu as deux options :

### Option 1 : Sans conteneur (l'ancien monde)
- Tu mets tout en vrac dans le camion
- Chaque objet bouge pendant le transport
- À l'arrivée, tu dois tout réorganiser
- Certaines choses sont cassées 😢

### Option 2 : Avec des conteneurs (Docker)
- Tu mets tout dans des boîtes standardisées
- Chaque boîte est isolée et protégée
- Tu sais exactement ce qu'il y a dans chaque boîte
- Le transport est sûr et rapide ✅

**Docker fait la même chose avec tes applications** : il les met dans des "conteneurs" 
standardisés qui fonctionnent partout de la même manière.

## 📦 Conteneur vs Machine Virtuelle

Quelle est la différence ?

### Machine Virtuelle (VM)
```
┌─────────────────────────┐
│   Application 1         │
│   ├── Librairies        │
│   └── Dépendances       │
├─────────────────────────┤
│   OS Complet (Ubuntu)   │  ← LOURD !
├─────────────────────────┤
│   Hypervisor            │
├─────────────────────────┤
│   Hardware              │
└─────────────────────────┘
```

### Conteneur Docker
```
┌─────────────────────────┐
│   Application 1         │
│   ├── Librairies        │
│   └── Dépendances       │
├─────────────────────────┤
│   Docker Engine         │  ← LÉGER !
├─────────────────────────┤
│   OS Hôte (Linux)       │
├─────────────────────────┤
│   Hardware              │
└─────────────────────────┘
```

**Résultat** : Les conteneurs sont **10x plus légers** et démarrent en **secondes** au lieu de minutes !

## 🎯 Pourquoi utiliser Docker ?

### ✅ Les avantages

1. **Portabilité** : "Ça marche sur ma machine" → "Ça marche partout" 🚀
2. **Isolation** : Chaque app dans son conteneur, zéro conflit
3. **Rapidité** : Lance une app en 2 secondes
4. **Reproductibilité** : Environnement identique dev/prod
5. **Scalabilité** : Lance 100 conteneurs en quelques secondes

### ❌ Les inconvénients

- Courbe d'apprentissage au début
- Nécessite Linux (ou WSL2 sur Windows)
- Peut consommer de l'espace disque

> **Mon avis** : Docker a révolutionné ma façon de travailler. Je ne peux plus m'en passer !

## 📥 Installation

### Sur Ubuntu/Debian

```bash
# Installation officielle
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Ajouter ton user au groupe docker (évite de faire sudo)
sudo usermod -aG docker $USER

# Redémarre ta session
newgrp docker
```

### Sur macOS

Télécharge [Docker Desktop](https://www.docker.com/products/docker-desktop)

### Sur Windows

1. Active WSL2
2. Installe [Docker Desktop](https://www.docker.com/products/docker-desktop)

### Vérification

```bash
docker --version
# Docker version 24.0.7, build afdd53b

docker run hello-world
```

Si tu vois "Hello from Docker!", c'est bon ! ✅

## 🚀 Ton premier conteneur

Lançons notre premier conteneur Nginx :

```bash
docker run -d -p 8080:80 --name mon-nginx nginx
```

Décortiquons cette commande :
- `docker run` : Lance un conteneur
- `-d` : Mode détaché (en arrière-plan)
- `-p 8080:80` : Map le port 8080 de ton PC vers le port 80 du conteneur
- `--name mon-nginx` : Donne un nom au conteneur
- `nginx` : L'image à utiliser

Ouvre ton navigateur sur `http://localhost:8080` → **Nginx tourne !** 🎉

## 🎨 Les commandes essentielles

### Gestion des conteneurs

```bash
# Lister les conteneurs actifs
docker ps

# Lister tous les conteneurs (même arrêtés)
docker ps -a

# Arrêter un conteneur
docker stop mon-nginx

# Démarrer un conteneur
docker start mon-nginx

# Redémarrer
docker restart mon-nginx

# Supprimer un conteneur
docker rm mon-nginx

# Supprimer un conteneur en cours d'exécution
docker rm -f mon-nginx
```

### Gestion des images

```bash
# Lister les images
docker images

# Télécharger une image
docker pull ubuntu

# Supprimer une image
docker rmi ubuntu

# Construire une image
docker build -t mon-app:1.0 .
```

### Logs et debug

```bash
# Voir les logs
docker logs mon-nginx

# Suivre les logs en temps réel
docker logs -f mon-nginx

# Entrer dans un conteneur
docker exec -it mon-nginx bash

# Voir les stats CPU/RAM
docker stats
```

## 🏗️ Créer ton propre conteneur

### Étape 1 : Créer un Dockerfile

```dockerfile
# Utilise une image de base
FROM ubuntu:22.04

# Installe des packages
RUN apt-get update && apt-get install -y \
    curl \
    vim \
    && rm -rf /var/lib/apt/lists/*

# Définit le répertoire de travail
WORKDIR /app

# Copie des fichiers
COPY . /app

# Expose un port
EXPOSE 3000

# Commande par défaut
CMD ["echo", "Hello from my container!"]
```

### Étape 2 : Construire l'image

```bash
docker build -t mon-image:latest .
```

### Étape 3 : Lancer le conteneur

```bash
docker run mon-image:latest
```

## 🎯 Exemple pratique : App Node.js

Créons une vraie application !

### 1. Crée ton app

**app.js**
```javascript
const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Hello Docker! 🐳');
});

app.listen(3000, () => {
  console.log('Server running on port 3000');
});
```

**package.json**
```json
{
  "name": "docker-app",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.0"
  },
  "scripts": {
    "start": "node app.js"
  }
}
```

### 2. Crée le Dockerfile

```dockerfile
FROM node:18-alpine

WORKDIR /app

# Copie package.json et installe les dépendances
COPY package*.json ./
RUN npm install

# Copie le reste des fichiers
COPY . .

EXPOSE 3000

CMD ["npm", "start"]
```

### 3. Build et Run

```bash
# Construire
docker build -t my-node-app .

# Lancer
docker run -d -p 3000:3000 --name node-app my-node-app

# Tester
curl http://localhost:3000
```

## 🔗 Docker Compose : Plusieurs conteneurs

Pour gérer plusieurs conteneurs ensemble, on utilise **Docker Compose**.

**docker-compose.yml**
```yaml
version: '3.8'

services:
  # App Node.js
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://db:5432/mydb
    depends_on:
      - db
  
  # Base de données PostgreSQL
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: mydb
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
```

Commandes Docker Compose :

```bash
# Lancer tous les services
docker-compose up -d

# Voir les logs
docker-compose logs -f

# Arrêter
docker-compose down

# Rebuild et relancer
docker-compose up -d --build
```

## 💾 Les volumes : Persister les données

Par défaut, les données dans un conteneur sont **perdues** quand tu le supprimes.
Solution : les **volumes** !

```bash
# Créer un volume
docker volume create mon-volume

# Utiliser un volume
docker run -d \
  -v mon-volume:/data \
  --name container-avec-volume \
  ubuntu

# Lister les volumes
docker volume ls

# Supprimer un volume
docker volume rm mon-volume
```

## 🎨 Tableau récapitulatif

| Commande | Description |
|----------|-------------|
| `docker run` | Lance un conteneur |
| `docker ps` | Liste les conteneurs actifs |
| `docker stop` | Arrête un conteneur |
| `docker rm` | Supprime un conteneur |
| `docker images` | Liste les images |
| `docker pull` | Télécharge une image |
| `docker build` | Construit une image |
| `docker logs` | Affiche les logs |
| `docker exec` | Exécute une commande dans un conteneur |

## 🚀 Exercice pratique

**Mission** : Crée une stack complète WordPress + MySQL avec Docker Compose.

<details>
<summary>Solution 👇</summary>

```yaml
version: '3.8'

services:
  wordpress:
    image: wordpress:latest
    ports:
      - "8080:80"
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: wp_user
      WORDPRESS_DB_PASSWORD: wp_password
      WORDPRESS_DB_NAME: wordpress
    volumes:
      - wordpress-data:/var/www/html
  
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wp_user
      MYSQL_PASSWORD: wp_password
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - db-data:/var/lib/mysql

volumes:
  wordpress-data:
  db-data:
```

Lance avec : `docker-compose up -d`
</details>

## 🎓 Bonnes pratiques

1. **Images multi-stage** : Réduis la taille de tes images
2. **Utilise .dockerignore** : N'inclus pas node_modules, .git, etc.
3. **Tag tes images** : `mon-app:v1.2.3` au lieu de `:latest`
4. **Nettoie régulièrement** : `docker system prune -a`
5. **Utilise des layers de cache** : Copie d'abord package.json avant le reste

## 🔥 Prochaines étapes

1. Apprends [Kubernetes](#) pour orchestrer des conteneurs à grande échelle
2. Découvre [Docker Hub](#) pour partager tes images
3. Explore les [multi-stage builds](#) pour optimiser tes images
4. Teste [Portainer](#) pour une interface graphique

## 📚 Ressources

- [Documentation Docker officielle](https://docs.docker.com)
- [Ma playlist YouTube Docker](#)
- [Docker Hub](https://hub.docker.com)
- [Awesome Docker](https://github.com/veggiemonk/awesome-docker)

---

**Félicitations !** Tu sais maintenant utiliser Docker. À toi de conteneuriser toutes tes apps ! 🐳
